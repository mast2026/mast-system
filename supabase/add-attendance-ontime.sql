-- =====================================================================
-- 출석 정시 기준 시각(ontime_at) 컬럼 추가
-- 이 시각 이후 출석 체크하면 지각으로 분류합니다.
--   - 지각 30분 이내: -1점
--   - 지각 30분 초과: -3점
-- 값을 비워두면 모임 '시작 일시(starts_at)'를 정시 기준으로 사용합니다.
-- Supabase SQL Editor에서 한 번 실행하세요.
-- =====================================================================
alter table public.activity_sessions
  add column if not exists ontime_at timestamptz;
